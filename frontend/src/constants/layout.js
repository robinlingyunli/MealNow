// Shared horizontal page padding so the navbar, page sections, and footer
// stay edge-aligned across the site.
export const PAGE_PADDING_X = "px-5 lg:px-32";

// Shared max content width so page content stays capped and centered on
// wide screens instead of stretching edge to edge.
export const PAGE_MAX_WIDTH = "max-w-screen-2xl mx-auto";
